# OpenChamber DFMEA Minimal Integration Plan

1. Keep OpenChamber as the generic OpenCode-facing shell.
2. Add a dedicated DFMEA domain package.
3. Add DFMEA project config keys and project action presets.
4. Add narrow DFMEA API endpoints later instead of rewriting core chat/session flows.
