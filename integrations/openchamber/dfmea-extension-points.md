# DFMEA Extension Points

Minimal first-version extension points:

1. Project-level DFMEA config and action presets.
2. Local project/workset context derived from the active OpenChamber project.
3. Review/apply flow surfaced through existing OpenChamber permission and diff primitives.
4. Narrow `/api/dfmea/*` endpoints added to the web server runtime.
