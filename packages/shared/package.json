{
  "name": "@dfmea/shared",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node ../../node_modules/vitest/vitest.mjs run \"src/**/*.test.ts\" --pool forks --maxWorkers=1 --no-file-parallelism",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  },
  "devDependencies": {
    "eslint": "^10.2.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
