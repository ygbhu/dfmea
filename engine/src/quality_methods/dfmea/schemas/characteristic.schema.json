{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Characteristic",
  "type": "object",
  "required": ["apiVersion", "kind", "metadata", "spec"],
  "properties": {
    "apiVersion": {"const": "quality.ai/v1"},
    "kind": {"const": "Characteristic"},
    "metadata": {
      "type": "object",
      "required": ["id"],
      "properties": {"id": {"pattern": "^CHAR-[0-9]{3,}$"}}
    },
    "spec": {"type": "object"}
  }
}
