{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FailureMode",
  "type": "object",
  "required": ["apiVersion", "kind", "metadata", "spec"],
  "properties": {
    "apiVersion": {"const": "quality.ai/v1"},
    "kind": {"const": "FailureMode"},
    "metadata": {
      "type": "object",
      "required": ["id"],
      "properties": {"id": {"pattern": "^FM-[0-9]{3,}$"}}
    },
    "spec": {"type": "object"}
  }
}
